import React from "react";

function NavBar() {
  // update the JSX being returned!
  return <nav>NavBar</nav>;
  <nav>
    <a href="#home"></a>
    <a href="#about"></a>
  </nav>
}

export default NavBar;
